const config = {
  apiUrl: '/api', // This will be proxied to http://localhost:5000 as configured in vite.config.ts
  defaultImagePath: '/totebag.png', // Fallback image path
};

export default config;
